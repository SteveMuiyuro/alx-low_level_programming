#include "main.h"
/**
  *_isdigit - This function checks for a digit (0 through 9).
  *@c: The vlue being evaluated
  *Return: 1 otherwise 0
  */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
