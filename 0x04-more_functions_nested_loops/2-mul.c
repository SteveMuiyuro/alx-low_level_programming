#include "main.h"
/**
  *mul - Function multiplies two intergers
  *@a: is the first value being evaluated
  *@b: is the econd value being evaluated
  *Return: Always 0
  */
int mul(int a, int b)
{
	result = a * b;

return (result);
}
