#include "main.h"
/**
  *rev_string - This function reverses a string
  *@s: This is a string pointer argument
  */
void rev_string(char *s)
{
	int i, j;

	j = strlen(s);

	for (i = j - 1; i >= 0; i--)
		_putchar(s[i];
}

