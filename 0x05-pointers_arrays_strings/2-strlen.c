#include "main.h"
#include <string.h>

/**
  *_strlen - This function prints the length of a string
  *@s: is the string pointer argument
  *Return: Returns the length of the target string
  */
int _strlen(char *s)
{

	return (strlen(s));
}
