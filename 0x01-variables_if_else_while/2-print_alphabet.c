#include <stdio.h>
#include <ctype.h>

/**
  *main - Entry point
  *
  *Return: 0 (Success)
  */
int  main(void)
{
	int b;

	for (b = 'a'; b <= 'z'; b++)
	{
		putchar(b);
		putchar('\n');

	}
	return (0);
}

